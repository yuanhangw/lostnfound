            <div class ="span4">

	            <% if signed_in? %>

            	<%= render 'shared/wolf_feed' %>

            	<% end %>

            </div>

            <div class ="span2">
            </div>

            <div class ="span3">
            </div>

            <div class ="span3"> 

	            <% if signed_in? %>

					<%= render 'wolves/wolf_form' %>

				<% else %>

		 			<%= render 'sessions/new' %>                
		            
					<%= render 'users/form_new' %>

				<% end %>

			</div>