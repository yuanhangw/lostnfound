            <div class ="span4">

	            <% if signed_in? %>

            	<%= render 'shared/wolf_feed' %>

            	<% else %>

            	<% end %>

            </div>

            <div class ="span2">
            </div>

            <div class ="span3">
            </div>

            <div class ="span3"> 

	            <% if signed_in? %>

	            <!-- need to implement friendly signin here -->
	            <!-- smokes has before_filter -->	  

	            <div class =" well well-small">          
					<%= render 'smokes/show' %>
				</div>
				
				<% else %>

		 			<%= render 'sessions/new' %>                
		            
					<%= render 'users/form_new' %>

				<% end %>

			</div>