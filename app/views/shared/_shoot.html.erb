
  <%= form_for current_user.shoots.build(:user_id => current_user.id, :smoke_id => smoke.id),:remote => true do |f| %>
    <div>
      <%= f.hidden_field :user_id %>
      <%= f.hidden_field :smoke_id %>
      <div class="field">
        <%= f.text_area :content, class: "redactor", :placeholder => "I have the answer !!!"%>
      </div>
    </div>

    <div class="buttomcontainer">
    <%= f.submit "Submit a tip", :class => "buttom" %>
    </div>

  <% end %>


          
<script>

   // $('textarea').blur(function() {
   //   $(this).closest('form').find('input[type="submit"]').show();
   //   $('textarea').animate({height: "3em"}, 750);
   // });

    $(".new_shoot").bind('ajax:beforeSend', function(){
      console.log("beforeSend");
    });


    $(".new_shoot").bind('ajax:success', function(){
      console.log("successful");
    });


    $(".new_shoot").bind('ajax:complete', function(){
      console.log("complete");
    });

   $(".new_shoot").bind('ajax:error', function(){
      console.log("errr");
    });

  


    // bind with ajax loading status. 
    // http://www.simonecarletti.com/blog/2010/06/unobtrusive-javascript-in-rails-3/

    // jQuery(function($) {
    //   // create a convenient toggleLoading function
    //   var toggleLoading = function() { $("#loading").toggle() };

    //   $("#tool-form")
    //     .bind("ajax:loading",  toggleLoading)
    //     .bind("ajax:complete", toggleLoading)
    //     .bind("ajax:success", function(event, data, status, xhr) {
    //       $("#response").html(data);
    //     });
    // });

</script>